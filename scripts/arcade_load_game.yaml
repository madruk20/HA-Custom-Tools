load_game:
  alias: Load Game
  fields:
    game_name:
      description: Name of the game
  sequence:
    - action: hassagent.send_command
      data:
        entity_id: switch.loadgame
        command: "{{ game_name }}"